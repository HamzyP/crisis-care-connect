// Wrapper component for hospital view app routes
// This allows it to be used in the root router
import App from './App';
import './hospital-view-styles.css';

const HospitalAppRoutes = () => {
  return <App />;
};

export default HospitalAppRoutes;

